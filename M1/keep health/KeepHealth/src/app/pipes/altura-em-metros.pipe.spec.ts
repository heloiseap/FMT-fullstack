import { AlturaEmMetrosPipe } from './altura-em-metros.pipe';

describe('AlturaEmMetrosPipe', () => {
  it('create an instance', () => {
    const pipe = new AlturaEmMetrosPipe();
    expect(pipe).toBeTruthy();
  });
});
